import 'package:flutter_test/flutter_test.dart';

void main() {
  group('Calories Estimation Page', () {
    test('[Calories] Display the recognized meal image', () {
      expect(true, true);
    });
    test(
        '[Calories] List each detected component with portion size and estimated calories',
        () {
      expect(true, true);
    });
    test(
        '[Calories] Automatically calculate and display total estimated calories for the whole meal',
        () {
      expect(true, true);
    });
    test(
        '[Calories] Allow the user to save the estimated meal records by pressing a Save button',
        () {
      expect(true, true);
    });
    test(
        '[Calories] Store meal history with date/calories and navigate to History page when saved',
        () {
      expect(true, true);
    });
    test(
        '[Calories] Allow user to switch to other main pages through the bottom navigation bar',
        () {
      expect(true, true);
    });
  });
}
